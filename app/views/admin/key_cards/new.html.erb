<h1>New keycard</h1>
<br /><br />
<%= simple_form_for(@keycard, :url => admin_key_card_path(@keycard), :html => {:class => 'form-horizontal'}) do |f| %>
  <%= f.input :key, :hint => 'Do not use a key number that may conflict with a student ID' %>
  <%= f.association :room, as: :radio_buttons %>
  <div class="control-group">
    <div class="controls"><%= f.button :submit, 'Create', :class => 'btn-primary' %></div>
  </div>
<% end %>

<div class="pull-right">
  <%= link_to 'Back', admin_key_cards_path, :class => 'btn' %>
</div>
